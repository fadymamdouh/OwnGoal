# رفع أون جول على استضافة مجانية

## الخلاصة: Render — الخطة المجانية

بلا كريدت كارد · 512 ميجا رام · 750 ساعة في الشهر · **WebSockets مدعومة**.

### ليه Render بالتحديد

معظم الاستضافات المجانية **مش هتشتغل معانا** أصلًا: Vercel و Netlify بيشغلوا
serverless functions، يعني مفيش عملية بتفضل شغالة ماسكة اتصال مفتوح — واللعبة
كلها قايمة على WebSocket وحالة في الميموري. فدول مستبعدين من الأول، مش تفضيل.

وفيه تفصيلة في توثيق Render تخدمنا بالظبط: الخدمة المجانية بتنام بعد **15 دقيقة
من غير أي trafic داخل**، و**رسائل WebSocket من اتصال قايم بتتحسب trafic داخل**.
والكلاينت بيبعت ping كل 25 ثانية، يعني:

- **ماتش شغال عمره ما ينام.**
- النوم بيحصل بس لما محدش يلعب — ووقتها مفيش أوضات تتخسر.
- أول زيارة بعد النوم بتستغرق دقيقة تقريبًا لحد ما الخدمة تصحى.

الشهر 744 ساعة، والباقة 750 — يعني تكفي حتى لو السيرفر مانامش ولا مرة.

## خطوات الرفع

1. ارفع الريبو على GitHub.
2. في Render: **New → Blueprint** واختار الريبو. ملف `render.yaml` فيه كل
   الإعدادات، فمش محتاج تظبط حاجة.
3. استنى البناء، وهياخد لينك زي `https://owngoal.onrender.com`.

خلاص. الـ HTTPS و الـ WSS بيتظبطوا لوحدهم، والكلاينت بيحوّل تلقائيًا لـ `wss://`
لما الصفحة تكون على HTTPS.

## حاجة لازم تعرفها قبل ما تنشر اللينك

**الأوضات عايشة في ميموري السيرفر.** يعني لو Render أعادت تشغيل الخدمة (نشر
جديد، أو نومة)، **كل الماتشات الشغالة بتضيع**. مفيش قاعدة بيانات في التصميم ده،
وده كان قرار صح للسرعة.

عمليًا مش مشكلة كبيرة: الماتش بياخد 12-20 دور (دقايق)، والتوكن في المتصفح بيرجّع
اللاعب لأوضته لو لسه موجودة. بس **متنشرش اللينك في مسابقة أو حاجة رسمية** قبل ما
نضيف تخزين — ووقتها Redis مجاني (Upstash أو Render Key Value) بيحل الموضوع.

## بدائل لو Render ضايقتك

| المنصة | الوضع | الملاحظة |
|---|---|---|
| **Northflank** | خدمات مجانية بتفضل صاحية | مفيش cold start — يستاهل التجربة لو الدقيقة دي مضايقاك |
| **Koyeb** | خدمة واحدة مجانية | scale-to-zero، نفس مشكلة النومة |
| **Oracle Cloud** | Always Free VM | **الأفضل على المدى الطويل ومجاني للأبد** — 4 كور ARM و24 جيجا رام، بس محتاج تظبط السيرفر بإيدك ويطلب كارت للتحقق |
| **Railway** | كريدت 5 دولار | مش مجاني فعليًا، الكريدت يخلص |
| Vercel · Netlify | ❌ | serverless — مبيدعموش WebSocket |
| Hugging Face Spaces | ❌ | Docker Spaces بقت محتاجة خطة مدفوعة |

لو اللعبة كبرت وعايز تشيل النومة خلاص، أرخص حل حقيقي هو **VPS بـ 4-5 دولار**
(Hetzner) — أو خطة Render المدفوعة بـ 7 دولار.

## تشغيل محلي

```
pip install -r requirements.txt
python scripts/server.py          # http://localhost:8000
```

للتجربة السريعة مع البوت بلا انتظار:

```
OG_BOT_DELAY=0 python scripts/server.py --port 8127
python scripts/test_server.py --port 8127
```
