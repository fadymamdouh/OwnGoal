{
  "rules": {
    ".read": false,
    ".write": false,

    "rooms": {
      "$code": {
        ".write": "auth != null && (data.child('meta').child('host').val() === auth.uid || (data.child('players').child(auth.uid).exists() && data.child('meta').child('hostLeftAt').exists() && data.child('meta').child('hostLeftAt').val() + 90000 < now))",

        "meta": {
          ".read": "auth != null",
          ".write": "auth != null && (!data.exists() || data.child('host').val() === auth.uid)"
        },

        "players": {
          ".read": "auth != null",
          "$uid": {
            ".write": "auth != null && ($uid === auth.uid || root.child('rooms').child($code).child('meta').child('host').val() === auth.uid)"
          }
        },

        "views": {
          "$uid": {
            ".read": "auth != null && auth.uid === $uid",
            ".write": "auth != null && (root.child('rooms').child($code).child('meta').child('host').val() === auth.uid || ($uid === auth.uid && !newData.exists()))"
          }
        },

        "actions": {
          ".read": "auth != null && root.child('rooms').child($code).child('meta').child('host').val() === auth.uid",
          "$pushId": {
            ".write": "auth != null && (!data.exists() || root.child('rooms').child($code).child('meta').child('host').val() === auth.uid)",
            ".validate": "newData.hasChildren(['uid','action']) && newData.child('uid').val() === auth.uid"
          }
        }
      }
    }
  }
}
